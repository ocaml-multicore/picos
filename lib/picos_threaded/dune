(library
 (name picos_threaded)
 (public_name picos.threaded)
 (optional)
 (libraries
  (re_export picos)
  (re_export picos.exn_bt)
  (select
   select.ml
   from
   (picos.select -> select.some.ml)
   (-> select.none.ml))
  threads.posix))
