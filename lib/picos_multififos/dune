(library
 (name picos_multififos)
 (public_name picos.multififos)
 (enabled_if
  (>= %{ocaml_version} 5.0.0))
 (libraries
  (re_export picos)
  (select
   select.ml
   from
   (picos.select -> select.some.ml)
   (-> select.none.ml))
  multicore-magic
  picos.thread
  picos.mpmcq))
