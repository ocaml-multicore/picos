(test
 (name main)
 (action
  (run %{test} -brief))
 (package picos)
 (libraries
  picos
  picos_mpsc_queue
  (select
   scheduler.ml
   from
   (picos.fifos -> scheduler.ocaml5.ml)
   (picos.threaded -> scheduler.ocaml4.ml))
  multicore-bench
  multicore-magic
  domain_shims))
