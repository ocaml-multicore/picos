(library
 (name picos_threaded)
 (optional)
 (libraries
  (re_export picos)
  (re_export exn_bt)
  (select
   select.ml
   from
   (picos_stdio.select -> select.some.ml)
   (-> select.none.ml))
  threads.posix))
