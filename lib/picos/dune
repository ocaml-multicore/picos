(library
 (name picos)
 (public_name picos)
 (libraries
  (re_export picos_exn_bt)
  picos_private_bootstrap
  (select
   picos.ml
   from
   (picos_private_ocaml4 -> picos.ocaml4.ml)
   (picos_private_ocaml5 -> picos.ocaml5.ml))
  (select
   intf.ml
   from
   (picos_private_ocaml4 -> intf.ocaml4.ml)
   (picos_private_ocaml5 -> intf.ocaml5.ml))))

(mdx
 (package picos)
 (deps
  (package picos))
 (libraries picos picos_exn_bt domain_shims)
 (files picos.mli))
