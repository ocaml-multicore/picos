(test
 (name main)
 (action
  (run %{test} -brief))
 (libraries
  picos
  picos.mpsc_queue
  picos.htbl
  picos.sync
  picos.domain
  picos.thread
  picos.stdio
  picos.select
  (select
   scheduler.ml
   from
   (picos.fifos -> scheduler.ocaml5.ml)
   (picos.threaded -> scheduler.ocaml4.ml))
  multicore-bench
  multicore-magic
  backoff))
